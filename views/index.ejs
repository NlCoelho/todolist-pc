<!DOCTYPE html>

<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="height=device-height, width=device-width, initial-scale=1.0, minimum-scale=1.0, target-densitydpi=device-dpi">
    <link rel="stylesheet" href="/style.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"/>
    <title>To Do LIst</title>
  </head>
  <body>
    <main>
      <container>
        <header>
          <input type="text" id="inputPesq" placeholder="PRODUTO" onkeyup="Pesq(this.value)" />
          <% if(1) { %>
          <form action="/create" method="post">
            <input type="text" name="fetes" id="fts" placeholder="PRODUTO" />
            <input type="text" name="cod" id="cod" placeholder="CÓDIGO" />
            <input type="number" name="config" id="config" min="0" max="6" />
            
            <button id="addButton" type="submit">Adicionar</button>
          </form>
          <% } else if (fetes != null) { %>
          <form action="/updateOne/<%= fetes._id %>" method="post">
            <input type="text" name="fetes" id="fetes" value="<%= fetes.fetes %>"/>
            <button id="addButton" type="submit">Atualizar</button>
          </form>
          <% } %>
        </header>

        <%- include('./components/modeloTabela.ejs') %>
        <% if(1) { %>
          <%- include('./components/modalSearch.ejs') %>
        <% } %>  
      </container>
      

    </main>
    <script src="/reload/reload.js"></script>
    <script src="script.js"></script>
  </body>
</html>
